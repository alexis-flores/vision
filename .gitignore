# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
*.whl

# Virtual environments
.venv/
venv/
env/

# Test / QA tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.hypothesis/
mutants/
.mutmut-cache

# Vendored Spinnaker SDK + FLIR examples (reference only — not part of this project)
Spinnaker/
Spinnaker-Examples-main/
Spinnaker_Download/
# Vendor PySpin wrapper + docs (reference). NOTE: a PySpin.py in the repo root
# SHADOWS the installed SDK on `import PySpin` from here — keep it out of the tree
# (move to a non-import-path dir) if you hit import issues.
PySpin.py
PySpinDoc.pdf
Spinnaker-Python-Programmer-Guide.pdf

# draw.io autosave backups
*.drawio.bkp
.$*

# OS / editor
.DS_Store
*.swp

# Claude Code: ignore only per-machine local settings; SHIP the shared
# CLAUDE.md (repo root) and .claude/skills/ so a cloned rig gets the kit.
.claude/settings.local.json

# Jupyter
.ipynb_checkpoints/
