# Core runtime dependencies for the vision system (camera backend + frame
# serving). These are sufficient to run app.py and the test suite.
#
# Optional extras (GUI, YAML configs, real hardware) are declared in
# pyproject.toml under [project.optional-dependencies]. Install them with:
#     pip install -e ".[gui]"      # PyQt6 live viewer
#     pip install -e ".[yaml]"     # YAML config files
#     pip install -e ".[dev]"      # test/lint tooling
#     pip install -e ".[all]"      # all of the above
#
# Note: the FLIR/Teledyne Spinnaker SDK (PySpin), required by
# spinnaker_driver.py for real BlackFly hardware, is NOT on PyPI. Install it
# from the vendor SDK package. The rest of the stack runs without it.

numpy>=1.24
opencv-python-headless>=4.6
